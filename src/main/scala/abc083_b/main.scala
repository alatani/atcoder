package abc083_b

// Some somes
// https://abs.contest.atcoder.jp/tasks/abc083_b

import scala.io.StdIn
object Main extends App {
  
  val line = StdIn.readLine().split(" ")
  val (n,a,b) = (line(0),line(1),line(2))
  
//  a <= sum(n) <= b
  
}
